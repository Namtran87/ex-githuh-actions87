name: First Workflow
on: [push, folk]
jobs: 
  first-job:
    runs-on:ubuntu-latest
    steps:
      - name: print greeting
        run: echo "Hello World"
      - name: Print goodbye
        run: echo "Done-yet"
